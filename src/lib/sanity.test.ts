import { describe, expect, it } from 'vitest';

// Placeholder so the test runner is proven wired up in Phase 0.
// Phase 1 replaces this with the shouldCapture and redaction suites.
describe('test harness', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
